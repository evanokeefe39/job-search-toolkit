# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Scraper output
*.csv
*.json
freework_jobs
jobs_ranked.csv

# OS
.DS_Store
Thumbs.db

# Environment
.env

# ATS pipeline run artifacts (regenerated on every run)
data/output/
data/metrics/
data/_tmp_*

# Node dependencies (vendored ATSFlow source)
node_modules/

# Personal application workspace — PUBLIC repo: PII, target companies, salary data
# Must stay ignored before any real content is created (gitignore is not retroactive)
resume/
applications/
rendercv_output/
