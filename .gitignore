# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
# Local project tooling registry (bundles a git checkout of this repo)
.pi/

# Scraper output
*.csv
*.json
freework_jobs
jobs_ranked.csv

# Apify actor config/schema are committed (the *.json glob above is for scraper output)
!actors/wttj/.actor/actor.json
!actors/wttj/.actor/INPUT_SCHEMA.json
!.omp-plugin/marketplace.json
!tests/fixtures/*.json

# OS
.DS_Store
Thumbs.db

# Environment
.env

# Runtime artifacts (regenerated on every run — never commit)
data/output/
data/metrics/
data/_tmp_*
data/market_*
data/outreach_*
data/contacts*
data/linkedin/
data/searxng/
# Quack serving stack (server, UI attach, refresh flag) + serving mirror — local runtime, may hold the quack token
# Never commit: data/_quack/attach.sql embeds the quack token and serve.db.* carries job data
#   (duckdb.exe binary + WAL are not covered by the *.db rule)
data/_quack/
data/warehouse/
node_modules/

# Actor-local run artifacts (regenerated per run)
actors/wttj/storage/

# Personal application workspace — PUBLIC repo: PII, target companies, salary data
# Must stay ignored before any real content is created (gitignore is not retroactive)
resume/
applications/
rendercv_output/
job_search_preferences.yaml
tracker.csv
# Session plans — may contain target-company info and personal resume analysis
tasks/plans/
# (config.yaml + TONE.txt moved into the package as tracked defaults)

# User override config (may hold API keys); template config.example.yaml is tracked
config.yaml
# User-facing resume-tailoring preferences; template tailor_resume_preferences.example.yaml is tracked
tailor_resume_preferences.yaml

# DuckDB gold layer artifact (rebuilt by `job-search-toolkit pipeline gold`)
data/gold/
*.db
