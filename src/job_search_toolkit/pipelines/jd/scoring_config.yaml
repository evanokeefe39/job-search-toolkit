# Default scoring weight set (version 1). Sum to 1.0.
# Tuned for: well-paid, not too demanding, flexible, interesting.
# Future --apply-calibration (later phase) writes new versions under versions/.
version: 1
weights:
  pay: 0.30
  flexibility: 0.25
  low_responsibility: 0.20
  tech_match: 0.15
  company_quality: 0.10
