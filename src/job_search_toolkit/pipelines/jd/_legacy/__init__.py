# Superseded stage scripts (kept for reference only).
# The Dagster graph in job_search_toolkit.pipelines.jd.assets replaces these.
# Do not run these in production; see assets/ for the canonical path.
#
# Note: stage5_score_analyze.py was promoted to ../score_engine.py — it is the
# only stage still used by the production scored_jobs asset.
